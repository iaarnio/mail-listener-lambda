{
  "name": "mail-listener-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "lambda-listener.js",
  "scripts": {
    "dist": "npm install && node mail-listener-lambda.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "netlify-lambda": "^0.4.0"
  },
  "dependencies": {
    "mail-listener2": "^0.3.1"
  }
}
